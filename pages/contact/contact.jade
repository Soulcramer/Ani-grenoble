#haut
	.container
		.row
			if (typeof(type) !== 'undefined')
				if (type == 'error') 
					.alert.fade.in.alert-danger
						a(class="close", href="#" data-miss="alert")
							i.font-icon-cross
						| Aïe! Le message n'as pas été envoyé, essayez de nous contacter via notre page facebook ou directement par e-mail.			
				if (type == 'empty') 
					.alert.fade.in.alert-warning
						a(class="close", href="#" data-miss="alert")
							i.font-icon-cross
						= msg
				if (type == 'success')
					.alert.fade.in.alert-success
						a(class="close", href="#" data-miss="alert")
							i.font-icon-cross
						| Super! Le message a été envoyé, nous vous contacterons bientôt.
			.col-sm-6.col-sm-offset-3
				.well
					h2 Nous contacter :
					h4.text-info Remplissez le formulaire ci-dessous, un email nous sera envoyé!
					form(name="contact-form", method="post", action="/contact")
						.row
							.form-group.col-sm-6
								label(for="name", class="control-label") Nom 
								input(name="name", id="name", type="text", class="form-control", placeholder="Jean", autofocus, required,value= (name ? "#{name}" : ''))
							.form-group.col-sm-6
								label(for="email", class="control-label") Email
								input(type="email",id="email", class="form-control" name="email" placeholder="jean@domain.com",required, value=(email ? "#{email}" : ''))
						.form-group
							label(for="message", class="control-label") Message
							textarea(name="message",id="message", class="form-control", rows="5", placeholder="Enter votre message ici…", required)!= (message ? message : '')
						input( id="company", type="text", placeholder="votre nom d'entreprise", name="company", size=40)
						.row
							button(type="submit", id="form-submit",class="btn btn-success btn-lg pull-right") Envoyer
							#msgSubmit.h3.text-center.hidden 
#bas
